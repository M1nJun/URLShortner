{
    "makefile.extensionOutputFolder": "./.vscode",
		"makefile.launchConfigurations": [
        {
            "cwd": "${fileDirname}",
            "binaryPath": "${fileDirname}/server",
            "binaryArgs": []
        }
    ]
}
